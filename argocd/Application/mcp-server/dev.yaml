apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-server-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/oluwaTG/azure-devops-scripts'
    targetRevision: main
    path: ./helm/Application/mcp-server
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: mcp-server-dev
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
