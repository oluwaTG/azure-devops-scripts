apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "movies-db.fullname" . }}-test-connection"
  labels:
    {{- include "movies-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "movies-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
