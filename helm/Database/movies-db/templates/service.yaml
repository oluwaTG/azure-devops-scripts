apiVersion: v1
kind: Service
metadata:
  name: {{ include "movies-db.fullname" . }}
  labels:
    {{- include "movies-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "movies-db.selectorLabels" . | nindent 4 }}
