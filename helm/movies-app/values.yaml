replicaCount: 1

image:
  repository: movies-app
  tag: local
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000
  targetPort: 8000

prometheus:
  enabled: true
  scrapePath: /metrics
  scrapePort: "8000"

env:
  DATABASE_URL: ""

resources: {}

labels:
  app: movies-app
